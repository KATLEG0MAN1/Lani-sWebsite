# Downloads

Drop the pack here as `lani-colors.zip`.

The icon on the orange gate links to `assets/downloads/lani-colors.zip`. Until that
file exists the icon shows "Pack drops soon" instead of serving a 404 — the check is
a HEAD request in `assets/js/gate.js`, so no code change is needed once you add it.
